#    -*- mode: org -*-

#+begin_src emacs-lisp :results silent
(setq-local gptel--system-message
            (with-temp-buffer
              (insert-file-contents "./PlanDeEscritura.org")
              (buffer-string)))

(setq-local gptel-context
            (list "../../org-lessons/S08-Lecc06.org"
		  "../../org-lessons/S09-Lecc07.org"))

#+end_src

Quiero que preparar la siguiente lección. 

Si necesitas más ficheros de referencia, más detalles o hacer sugerencias específicas sobre esos gráficos u otros aspectos, dímelo. 

