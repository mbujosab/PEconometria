Ejecuto el siguiente bloque para que tengas presente el plan de escritura y el contexto de esta tarea:

#+begin_src emacs-lisp :results silent
(setq-local gptel--system-message
            (with-temp-buffer
              (insert-file-contents "./PlanDeEscritura.org")
              (buffer-string)))
#+end_src

Y también ejecuto el siguiente bloque para que conozcas el contenido del las dos últimas lecciones (la 3 y 4, correspondientes a las sesiones 3 y 5):


#+begin_src emacs-lisp :results silent
(setq-local gptel-context
            (list "./S05-Lecc04.org"
		  "./S06-Lecc05.org"))
#+end_src


Vamos a escribir el notebook pendiente. Quiero que sea un documento orgmode, que exportaré como Notebook de Jupyter usando https://github.com/jkitchin/ox-ipynb desde emacs. El código debe ser python y el objetivo es poder visualizar en 3D la recta correspondiente a los múltiplos del vector (1,1,1). Poder indicar un vector cualquiera de R3, por ejemplo el (1,2,3) y que se vea su proyección sobre sobre la recta, y su proyección sobre el plano perpendicular a dicha recta. En las figuras de las lecciones en 2D, se muestra una cuadrícula que permite contar las diagonales, y por tanto las normas de los vectores. No sé si es posible lograr algo parecido en 3D (sería cubos cuasi-transparentes de lado 1). No sé que librerías deben usarse, ni si es posible hacer algo así en un notebook de python. Lo ideal seria poder cambias la orientación de las figuras, (el punto de vista del observador) de manera interactiva. No sé si esto es posible. 

Por otra parte, sería estupendo poder representar simultáneamente dos vectores de R3 para ver sus proyecciones; especialmente sobre el plano ortogonal a (1,1,1) para apreciar el ángulo de sus respectivos vectores en desviaciones respecto a la media.

No sé si me he explicado. Si tienes preguntas, hazlas antes de escribir nada. Si conviene cambiar de modelo para acometer esta tarea, dímelo.+

